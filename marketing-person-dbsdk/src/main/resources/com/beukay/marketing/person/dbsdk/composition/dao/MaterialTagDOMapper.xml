<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beukay.marketing.person.dbsdk.composition.dao.MaterialTagDOMapper">

    <resultMap id="BaseResultMap" type="com.beukay.marketing.person.dbsdk.composition.model.MaterialTagDO">
        <id     column="id"                 property="id"/>
        <result column="name"               property="name"/>
        <result column="category"           property="category"/>
        <result column="color"              property="color"/>
        <result column="description"        property="description"/>
        <result column="nezha_tenant_code"  property="nezhaTenantCode"/>
        <result column="is_deleted"         property="isDeleted"/>
        <result column="create_at"          property="createAt"/>
        <result column="create_by"          property="createBy"/>
        <result column="create_name"        property="createName"/>
        <result column="update_at"          property="updateAt"/>
        <result column="update_by"          property="updateBy"/>
        <result column="update_name"        property="updateName"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, category, color, description,
        nezha_tenant_code, is_deleted, create_at, create_by, create_name, update_at, update_by, update_name
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO material_tag
            (name, category, color, description,
             nezha_tenant_code, is_deleted, create_at, create_by, create_name, update_at, update_by, update_name)
        VALUES
            (#{name}, #{category}, #{color}, #{description},
             #{nezhaTenantCode}, #{isDeleted}, #{createAt}, #{createBy}, #{createName}, #{updateAt}, #{updateBy}, #{updateName})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM material_tag
        WHERE is_deleted = 0 AND id = #{id}
        LIMIT 1
    </select>

    <select id="selectByNameAndCategory" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM material_tag
        WHERE is_deleted = 0 AND name = #{name} AND category = #{category}
        LIMIT 1
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM material_tag
        WHERE is_deleted = 0
          AND id IN
          <foreach collection="ids" item="i" open="(" separator="," close=")">#{i}</foreach>
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM material_tag
        WHERE is_deleted = 0
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY category ASC, id ASC
    </select>

    <update id="softDelete">
        UPDATE material_tag
        SET is_deleted = 1, update_at = CURRENT_TIMESTAMP
        WHERE is_deleted = 0 AND id = #{id}
    </update>

</mapper>
