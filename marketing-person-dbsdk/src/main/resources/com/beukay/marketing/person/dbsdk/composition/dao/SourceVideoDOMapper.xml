<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beukay.marketing.person.dbsdk.composition.dao.SourceVideoDOMapper">

    <resultMap id="BaseResultMap" type="com.beukay.marketing.person.dbsdk.composition.model.SourceVideoDO">
        <id     column="id"                 property="id"/>
        <result column="oss_key"            property="ossKey"/>
        <result column="original_name"      property="originalName"/>
        <result column="duration_ms"        property="durationMs"/>
        <result column="file_size"          property="fileSize"/>
        <result column="width"              property="width"/>
        <result column="height"             property="height"/>
        <result column="segments_json"      property="segmentsJson"/>
        <result column="status"             property="status"/>
        <result column="nezha_tenant_code"  property="nezhaTenantCode"/>
        <result column="is_deleted"         property="isDeleted"/>
        <result column="create_at"          property="createAt"/>
        <result column="create_by"          property="createBy"/>
        <result column="create_name"        property="createName"/>
        <result column="update_at"          property="updateAt"/>
        <result column="update_by"          property="updateBy"/>
        <result column="update_name"        property="updateName"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, oss_key, original_name, duration_ms, file_size, width, height, segments_json, status,
        nezha_tenant_code, is_deleted, create_at, create_by, create_name, update_at, update_by, update_name
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO source_video
            (oss_key, original_name, duration_ms, file_size, width, height, segments_json, status,
             nezha_tenant_code, is_deleted, create_at, create_by, create_name, update_at, update_by, update_name)
        VALUES
            (#{ossKey}, #{originalName}, #{durationMs}, #{fileSize}, #{width}, #{height}, #{segmentsJson}, #{status},
             #{nezhaTenantCode}, #{isDeleted}, #{createAt}, #{createBy}, #{createName}, #{updateAt}, #{updateBy}, #{updateName})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM source_video
        WHERE is_deleted = 0 AND id = #{id}
        LIMIT 1
    </select>

    <sql id="PageWhere">
        WHERE is_deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="name != null and name != ''">
            AND original_name LIKE CONCAT('%', #{name}, '%')
        </if>
    </sql>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM source_video
        <include refid="PageWhere"/>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM source_video
        <include refid="PageWhere"/>
    </select>

    <update id="updateSegments">
        UPDATE source_video
        SET segments_json = #{segmentsJson}, update_at = CURRENT_TIMESTAMP
        WHERE is_deleted = 0 AND id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE source_video
        SET status = #{status}, update_at = CURRENT_TIMESTAMP
        WHERE is_deleted = 0 AND id = #{id}
    </update>

    <update id="updateProbe">
        UPDATE source_video
        SET duration_ms = #{durationMs}, width = #{width}, height = #{height},
            update_at = CURRENT_TIMESTAMP
        WHERE is_deleted = 0 AND id = #{id}
    </update>

    <update id="softDelete">
        UPDATE source_video
        SET is_deleted = 1, update_at = CURRENT_TIMESTAMP
        WHERE is_deleted = 0 AND id = #{id}
    </update>

</mapper>
