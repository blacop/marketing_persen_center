<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.beukay</groupId>
        <artifactId>marketing-person-center</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <groupId>com.beukay</groupId>
    <artifactId>marketing-person-domain</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>marketing-person-domain</name>
    <description>Domain model layer</description>

    <dependencies>
        <!-- COLA components -->
        <dependency>
            <groupId>com.alibaba.cola</groupId>
            <artifactId>cola-component-domain-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cola</groupId>
            <artifactId>cola-component-exception</artifactId>
        </dependency>
        <!-- COLA components End-->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-log4j2</artifactId>
        </dependency>

        <dependency>
            <groupId>com.beukay</groupId>
            <artifactId>ai-exception</artifactId>
        </dependency>
        <dependency>
            <groupId>com.beukay</groupId>
            <artifactId>ai-entity</artifactId>
        </dependency>

        <!-- composition 模块：domain 引用 client 枚举 -->
        <dependency>
            <groupId>com.beukay</groupId>
            <artifactId>marketing-person-client</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- RenderJobServiceImpl plan_snapshot JSON 序列化 -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <configuration>
                        <skip>true</skip>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
